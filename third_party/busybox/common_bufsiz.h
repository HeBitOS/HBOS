/* HBOS: empty stub. Real upstream's common_bufsiz.h declares a shared
 * static scratch buffer (bb_common_bufsiz1) used by several applets'
 * disabled-here feature paths (e.g. cat's ENABLE_FEATURE_CATV block,
 * compiled out — see libbb.h). Nothing currently vendored needs it for
 * real. */

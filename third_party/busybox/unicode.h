/* HBOS: empty stub. init_unicode()/unicode_status/UNICODE_ON are declared
 * directly in libbb.h (this port has no real Unicode support — see the
 * comment there). Real upstream's unicode.h is a much larger optional
 * subsystem header; nothing here needs it. */

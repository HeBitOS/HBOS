# HBOS 软件模块化、窗口控件与 Chromium 兼容层长期路线图

> 创建日期：2026-07-27
> 路线性质：长期规划，按阶段验收，不要求连续一次性完成
> 总体方向：先完成 HBOS 软件模块化和 GUI 公共能力，再建立 Chromium 平台兼容层，最后验证最小内容壳
> v1 预计总工作量：约 56–86 人周（14–22 人月）；2–3 人稳定投入时约 6–10 个月

## 2026-07-28 实施顺序调整

根据当前决策，暂缓 Chromium 源码迁入与交叉构建，先完成可复用的系统边界：

1. 优先发布用户态窗口控件 API，并用独立 `.hax` 示例验证。
2. 同时建立可独立启动的 no-GUI 版本，确保内核不反向依赖桌面或应用。
3. 采用“两个物理仓库、三个逻辑组件”：内核仓库；GUI 仓库中的 GUI
   runtime/桌面与独立应用包。正式仓库名称确定前不移动历史。
4. 阶段 4、5 当前成果属于提前实施的基础子集，不代表窗口 API v2 或完整控件库
   已经验收完成。

具体拆仓边界见 `docs/REPOSITORY_SPLIT_BOUNDARIES.md`。

## 总体目标

1. 将目前集中在 `src/tools/gui.c` 和全局 GUI 状态中的软件拆成独立、可注册、可测试的模块。
2. 将现有窗口绘图原语升级为稳定、版本化、面向用户态应用的窗口与控件 API。
3. 建立 Chromium 所需平台能力到 HBOS 系统调用、用户态 libc、窗口系统和网络栈的转译层。
4. 最终运行一个基于 Chromium 组件的最小内容壳，能够显示受控的本地及网络 HTML/CSS 页面。

## 重要边界

- 本路线中的“转译”是把 Chromium 依赖的平台接口适配到 HBOS，不是自动转换 Chromium 源代码。
- v1 不承诺运行完整 Chrome，也不承诺兼容所有现代网站。
- v1 优先采用 CPU 软件渲染；GPU 加速、音视频、WebRTC、扩展、打印及完整 Chromium 沙箱不在首期范围内。
- HBOS 内核继续使用 C99；Chromium 用户态组件允许使用其要求的 Clang 和现代 C++ 工具链。
- 每个阶段必须独立验收。上一阶段未通过稳定性验收，不进入依赖它的下一阶段。

---

## 阶段 1：建立基线、边界和自动验证

**预计工作量：2–3 人周**

### 目标

冻结当前功能基线，记录软件、GUI、系统调用和浏览器的实际能力，为后续拆分提供可比较的结果。

### 主要工作

- 盘点所有内置 GUI 软件、Shell 工具、用户态程序、窗口调用和浏览器功能。
- 为模块、窗口服务、网络、TLS 和现有浏览器建立能力清单。
- 建立 BIOS、UEFI、QEMU、VMware/VirtualBox 的最小冒烟测试矩阵。
- 记录内存占用、启动时间、窗口打开/关闭次数和网页加载结果等基准。
- 确定模块 ABI、GUI ABI 和 Chromium 兼容层的版本策略。

### 交付物

- 当前能力与缺口清单。
- 自动构建及启动冒烟脚本。
- 模块/API 版本约定和架构说明。

### 验收条件

- BIOS 与 UEFI 构建均可重复完成。
- 桌面、现有软件、窗口应用和浏览器具备可重复的基线测试。
- 后续阶段能够判断功能退化、性能退化和内存增长。

---

## 阶段 2：升级应用模块契约与状态模型

**预计工作量：3–4 人周**

### 目标

把现有 `gui_app_module_t` 扩展为完整的软件生命周期接口，并消除应用对全局 GUI 状态的结构性依赖。

### 主要工作

- 模块接口增加 `create`、`destroy`、`draw`、`event`、`tick`、`resize`、`suspend` 和 `resume`。
- 每个应用实例持有独立私有状态，桌面只保存模块句柄和通用窗口状态。
- 统一键盘、鼠标、窗口、计时器和关闭事件格式。
- 为旧模块提供临时兼容适配器，避免一次性迁移造成大范围破坏。
- 明确模块的内存、任务、文件句柄和窗口资源所有权。

### 交付物

- 应用模块 ABI v2。
- 模块注册表和实例生命周期管理器。
- 一个完整迁移的示范应用。

### 验收条件

- 示范应用可以创建、关闭、重新打开并正确释放资源。
- 同一模块的两个实例状态互不污染。
- 旧模块通过兼容适配器继续运行。

---

## 阶段 3：完成所有软件模块化

**预计工作量：4–6 人周**

### 目标

将内置软件从桌面主循环和大型源文件中拆出，使每个软件能够独立维护、注册和测试。

### 主要工作

- 拆分浏览器、记事本、代码编辑器、诊断、贪吃蛇和 UWC 等剩余内置应用。
- 已拆分的软件统一迁移到模块 ABI v2。
- Shell 工具、内置 GUI 软件和用户态程序使用清晰的注册入口与元数据。
- 桌面主循环只保留窗口调度、输入路由、合成、任务栏和模块调用。
- 将应用专用缓存、静态变量和辅助函数移入所属模块。

### 交付物

- 完整的软件模块目录和统一注册表。
- 精简后的桌面协调层。
- 软件模块开发模板及接入说明。

### 验收条件

- 当前所有软件功能保持可用。
- 新增一个简单应用时不需要修改桌面事件主循环。
- 关闭模块后不存在遗留窗口、任务、输入焦点或明显内存泄漏。

---

## 阶段 4：发布版本化窗口 API v2

**预计工作量：4–6 人周**

### 目标

在保持旧 `HAX_SYS_WIN_*` 可用的前提下，提供能承载通用 GUI 软件和浏览器表面的窗口 ABI。

### 主要工作

- 增加带结构体大小和 ABI 版本的窗口创建接口。
- 支持标题更新、移动/缩放通知、最小化、最大化、焦点和关闭状态。
- 增加 ARGB 位图上传、脏矩形提交、裁剪区域和批量绘图命令。
- 扩充窗口事件队列，并定义队列满、窗口销毁和应用异常退出行为。
- 移除“一任务只能拥有一个窗口”的接口假设，改为显式窗口句柄。
- 保留旧 API 包装层，旧 `.hax` 应用无需立即重编译。

### 交付物

- 窗口 ABI v2 头文件、系统调用实现和用户态包装。
- 旧窗口 ABI 到 v2 的兼容层。
- 多窗口、缩放和位图提交示例程序。

### 验收条件

- 一个用户态进程能够同时创建并独立控制多个窗口。
- 窗口缩放、焦点切换、关闭和异常退出不会破坏桌面。
- 局部更新不会强制复制或重绘整个窗口表面。

---

## 阶段 5：建立标准窗口控件库

**预计工作量：5–7 人周**

### 目标

让应用通过公共控件快速构建可用界面，不再重复手写按钮、输入框和命中检测。

### 主要工作

- 建立控件树、父子关系、控件句柄和统一事件派发。
- 首批控件包括标签、按钮、文本框、复选框、列表、滚动条、菜单、图片和自定义画布。
- 实现焦点、Tab 顺序、鼠标捕获、UTF-8 输入、文本选择和剪裁。
- 提供行、列、停靠或网格等最小布局能力。
- 抽离颜色、字号、间距和交互状态为主题令牌。
- 选择两个现有应用迁移为只使用公共控件 API。

### 交付物

- GUI 控件库 v1 和公共头文件。
- 控件展示程序及两个迁移完成的真实应用。
- 控件行为、布局和事件说明。

### 验收条件

- 示例应用不使用桌面内部绘图或命中检测函数。
- 键盘、鼠标、缩放和不同字号下控件均可操作。
- 控件主题能够统一切换，而无需逐应用修改颜色常量。

---

## 阶段 6：重构现有轻量浏览器

**预计工作量：4–6 人周**

### 目标

保留当前浏览能力，同时形成可被 Chromium 后端逐步替换的浏览器外壳和清晰管线。

### 主要工作

- 拆分 URL、导航历史、HTTP/TLS、HTML、CSS、布局、图片、绘制和浏览器界面模块。
- 浏览器窗口改用模块 ABI v2、窗口 API v2 和公共控件。
- 定义浏览器外壳与渲染后端之间的接口：导航、加载状态、文档尺寸、绘制表面和输入事件。
- 将网络错误、TLS 错误、解析错误和资源不足错误标准化。
- 建立本地 HTML/CSS、重定向、图片、滚动、链接和失败场景测试集。

### 交付物

- 模块化轻量浏览器。
- 稳定的浏览器外壳—渲染后端接口。
- 可离线运行的浏览器回归页面集合。

### 验收条件

- 当前支持的页面能力不得明显退化。
- 浏览器 UI 不直接依赖 HTML/CSS 解析器内部结构。
- 可用测试后端替换当前渲染器并显示固定测试页面。

---

## 阶段 7：补齐 Chromium 平台兼容基础

**预计工作量：8–12 人周**

### 目标

建立 Chromium 平台调用到 HBOS 内核、系统调用和用户态 libc 的转译层。

### 主要工作

- 补齐线程、互斥量、条件变量、线程局部存储、原子操作和等待/唤醒语义。
- 完善虚拟内存、共享内存、文件映射、保护属性和大块地址空间管理。
- 完善文件、目录、时间、随机数、环境、进程、socket 和事件等待接口。
- 为字体发现、字形栅格化、剪贴板、窗口表面和输入法定义平台接口。
- 为所有未实现能力提供明确错误和能力查询，禁止空实现返回成功。
- 增加压力测试：并发、内存映射、进程退出、IPC、网络中断和资源回收。

### 交付物

- `chromium_compat` 平台库。
- HBOS 用户态 libc/POSIX 能力扩展。
- Chromium 平台能力支持矩阵及自动测试。

### 验收条件

- 平台测试程序通过线程、内存、文件、网络、事件和窗口测试。
- 失败路径能返回确定错误，不导致内核崩溃或永久阻塞。
- 长时间压力测试后任务、映射、文件句柄和窗口资源可回收。

---

## 阶段 8：建立 Chromium 交叉构建与基础组件

**预计工作量：8–12 人周**

### 目标

让选定版本的 Chromium 基础组件能够面向 HBOS 编译、链接和运行。

### 主要工作

- 固定一个 Chromium 长期支持基线版本，记录提交号和补丁集。
- 建立 HBOS sysroot、Clang 工具链、GN 平台配置和 Ninja 构建流程。
- 先移植 `base` 必需子集、`partition_alloc`、URL、字符编码和必要第三方库。
- 将所有 HBOS 专用补丁集中到独立平台目录或可重放补丁集。
- 增加上游版本差异检查，避免补丁散落后无法升级。
- 生成可在 HBOS 启动的组件自测程序。

### 交付物

- 可重复的 Chromium 交叉构建工具链。
- 固定版本、补丁清单和升级说明。
- 在 HBOS 运行的 Chromium 基础组件测试程序。

### 验收条件

- 从干净环境能够按文档重复构建相同产物。
- 基础组件自测能在 ring3 中运行并正常退出。
- HBOS 平台修改不会直接混入无关 Chromium 上游目录。

---

## 阶段 9：运行最小 Chromium 内容壳

**预计工作量：12–20 人周**

### 目标

运行基于 Chromium 组件的最小内容壳，并通过浏览器外壳显示受控页面。

### 主要工作

- 优先采用受限单进程模式接通内容初始化、事件循环、字体和软件绘制。
- 将 Chromium 输出的 CPU 位图通过窗口 API v2 提交到浏览器窗口。
- 接通键盘、鼠标、滚轮、窗口缩放和页面重绘。
- 先支持本地静态 HTML/CSS 测试页，再接入受控 HTTP/HTTPS 页面。
- 对缺失的 V8、多进程、GPU 和媒体能力设置明确启动开关与错误提示。
- 对启动时间、峰值内存、页面切换和异常恢复进行测量。

### 交付物

- HBOS Chromium 最小内容壳。
- 浏览器外壳的 Chromium 渲染后端。
- 本地及网络页面演示和性能报告。

### 验收条件

- 能稳定显示本地 HTML/CSS 页面并完成滚动、点击和文本输入。
- 能加载至少一个受控 HTTPS 测试站点。
- 页面或渲染任务失败后桌面仍可使用，浏览器能够关闭或重新启动。
- 不支持的网页能力会明确报告，而不是卡死或破坏系统。

---

## 阶段 10：稳定化、发布与下一阶段决策

**预计工作量：6–10 人周**

### 目标

将前九阶段成果整理为可长期维护的 v1，并用数据决定是否继续追求完整 Chromium。

### 主要工作

- 修复回归、资源泄漏、竞争、死锁、崩溃和异常退出问题。
- 完成模块 SDK、窗口/控件 API、兼容层、构建工具链和调试文档。
- 建立持续构建、ABI 兼容检查、浏览器页面回归和性能基准。
- 制作可复现演示镜像，并验证 BIOS、UEFI 和主要虚拟机环境。
- 评估现代网站兼容率、内存要求、启动耗时、维护成本和上游升级难度。
- 根据评估结果选择下一路线：继续 Blink/V8、多进程沙箱与 GPU，或长期保留“轻量浏览器 + 可选 Chromium 内容壳”双后端。

### 交付物

- HBOS 软件模块化与 Chromium 兼容层 v1。
- SDK、构建、测试、调试、补丁维护和升级文档。
- 下一版本可行性报告与明确决策记录。

### 验收条件

- 所有阶段性自动测试和启动矩阵通过。
- 旧应用兼容策略、ABI 版本和弃用周期有明确文档。
- v1 能在目标最低配置上连续运行，重复开关软件和加载测试页面不崩溃。
- 是否进入“完整 Chromium”开发有基于测试数据的正式结论。

---

## 阶段依赖关系

```text
阶段 1
  ↓
阶段 2 → 阶段 3
  ↓        ↓
阶段 4 → 阶段 5 → 阶段 6
  ↓                  ↓
阶段 7 → 阶段 8 → 阶段 9
                      ↓
                   阶段 10
```

阶段 3 与阶段 4 可以在模块 ABI 确定后部分并行；阶段 7 的内核和 libc 工作可以与阶段 6 的浏览器拆分部分并行。阶段 8、9 必须在平台兼容能力通过测试后推进。

## 长期进度记录

执行时只更新下表状态，并在对应阶段下补充实际结果；不要删除原始目标和验收条件。

| 阶段 | 状态 | 开始日期 | 完成日期 | 实际投入 | 备注 |
|---|---|---|---|---:|---|
| 1. 基线与验证 | 已完成 | 2026-07-27 | 2026-07-27 | 1 次实施会话 | 静态基线、BIOS/UEFI 构建及完整 smoke 矩阵通过 |
| 2. 模块契约与状态 | 未开始 | — | — | — | — |
| 3. 所有软件模块化 | 进行中 | 2026-07-28 | — | 1 次实施会话 | HAX/注册式应用已可从镜像解绑并从外部 `APP_DIR` 构建；`tools/gui.c` 中的遗留内置应用仍待迁移 |
| 4. 窗口 API v2 | 基本完成 | 2026-07-28 | — | 2 次实施会话 | 已有版本化结构、显式代数句柄、多窗口、状态/几何/焦点事件、ARGB、批量绘制、脏矩形与 no-GUI 能力查询；桌面端最大化布局和更细粒度合成仍需继续 |
| 5. 标准控件库 | 进行中 | 2026-07-28 | — | 4 次实施会话 | HIVE 1.3 已有父子控件树/相对坐标/级联状态与子树删除、布局、首批控件、完整鼠标状态，以及 UTF-8 光标和键盘/鼠标文本选择；公开测试覆盖基础交互，父级绘制剪裁和两个真实应用迁移待完成 |
| 6. 轻量浏览器重构 | 未开始 | — | — | — | — |
| 7. Chromium 平台兼容基础 | 未开始 | — | — | — | — |
| 8. Chromium 交叉构建 | 未开始 | — | — | — | — |
| 9. 最小内容壳 | 未开始 | — | — | — | — |
| 10. 稳定化与发布决策 | 未开始 | — | — | — | — |

## 风险控制原则

- 不为了 Chromium 提前破坏现有轻量浏览器；在阶段 9 验收前保留原后端。
- 不在内核中直接堆放 Chromium 专用逻辑，平台适配主要位于用户态兼容层。
- 不接受无法自动验证的大规模拆分；每迁移一个模块都补充对应测试。
- 不以“能够编译”代替“能够运行和释放资源”的验收。
- 若阶段 7 或阶段 8 的成本显著超过估算，先重新评估目标，再决定是否投入完整 Blink/V8。
